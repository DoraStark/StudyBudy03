<div class="row">
  {{#each users}}
    <div class="col-md-4">
      <div class="p-3 m-2 bg-white shadow-sm border rounded">
        <h5>{{name}}</h5>
        <p>{{skills}}</p>
        <a href="#" class="btn btn-sm btn-outline-primary">Profile</a>
        <a href="#" class="btn btn-sm btn-outline-primary">Nachricht</a>
      </div>
    </div>
  {{/each}}
</div>
